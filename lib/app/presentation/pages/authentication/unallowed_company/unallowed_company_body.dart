import 'package:flutter/material.dart';

class UnallowedCompanyBody extends StatelessWidget {
  const UnallowedCompanyBody({super.key});
  @override
  Widget build(BuildContext context) {
    return const Column(
      children: <Widget>[
        //
      ],
    );
  }
}
